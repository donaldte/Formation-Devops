IWG, VPN gateway (VPN Gateway), Route table, Nat, Nacl, SG, subnet public-prive, vpc-endpoint, vpc-peering, 

auto-scaling group: min_values:1 , desired_capacity:2 max_capacity:4

chaque region a un VPC par defaut

par region 5 vpc: 200 subnets

translation: ip-prive -- > nat__signature --> ip-pubic